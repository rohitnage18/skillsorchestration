{
  "name": "skill-orchestration-repo",
  "private": true,
  "type": "module",
  "scripts": {
    "verify:repo": "npm --prefix conductor-app test && npm --prefix conductor-app run build && npm --prefix skills-mcp-server run build && npm --prefix skills-vscode-extension run compile-check && npm --prefix skills-vscode-extension run build"
  }
}
